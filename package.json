{
  "name": "aethercycle-protocol",
  "version": "1.0.0",
  "description": "Autonomous DeFi Protocol with Perpetual Economic Engine",
  "license": "AGPL-3.0",
  "keywords": [
    "defi",
    "autonomous",
    "aethercycle",
    "perpetual-engine"
  ],
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deployment/deploy.js --network localhost",
    "deploy:testnet": "hardhat run scripts/deployment/deploy.js --network base_sepolia",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "size": "hardhat size-contracts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0",
    "hardhat-contract-sizer": "^2.10.0",
    "solidity-coverage": "^0.8.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0"
  }
}
